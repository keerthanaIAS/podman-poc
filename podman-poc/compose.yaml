services:
  app:
    build: .
    container_name: podman-app
    ports:
      - "3000:3000"
    depends_on:
      - mongodb

  mongodb:
    image: mongo:8
    container_name: podman-mongodb
    ports:
      - "27017:27017"