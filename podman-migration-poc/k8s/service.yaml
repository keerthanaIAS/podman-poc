apiVersion: v1
kind: Service
metadata:
  name: cube-root-ms
  namespace: cube-poc

spec:
  selector:
    app: cube-root-ms

  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001

  type: NodePort